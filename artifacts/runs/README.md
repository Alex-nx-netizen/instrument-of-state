# Governed Run Artifacts

Store governed run artifacts here.

Recommended workflow:

1. Copy `contracts/run-artifact.template.json`
2. Rename it to `<YYYY-MM-DD>-<short-slug>.json`
3. Fill the packet chain during the run
4. Keep `summaryPacket.publicReady` and `writebackPacket` updated before close-out
